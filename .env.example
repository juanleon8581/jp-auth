# =============================================================================
# JP Auth API - Environment Variables Configuration
# =============================================================================
# This file serves as a template for environment variables.
# Copy this file to .env for local development.
# 
# Multi-Environment Support:
# - .env.dev (development)
# - .env.qa (quality assurance)
# - .env.prod (production)
# =============================================================================

# Server Configuration
NODE_ENV="dev"
PORT=3000

# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/jp_auth_db?schema=public"
# DATABASE_PASSWORD="your-database-password"  # Optional: separate password if needed
# DIRECT_URL="postgresql://username:password@localhost:5432/jp_auth_db"  # Optional: direct connection URL

# Supabase Configuration
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_ANON_KEY="your-supabase-anon-key"
# SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"  # Optional: for admin operations

# JWT Configuration
JWT_SECRET="your-jwt-secret-key-minimum-32-characters-long"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="8d"

# API Configuration
API_VERSION="v1"
# ADMIN_API_KEY="your-admin-api-key-for-app-registration"  # Optional: for admin endpoints

# Rate Limiting
RATE_LIMIT_WINDOW_MS=600000
RATE_LIMIT_MAX_REQUESTS=5

# CORS Configuration
CORS_ORIGIN="http://localhost:3000,http://localhost:3001"

# Logging Configuration
LOG_LEVEL="info"
LOG_FILE="logs/app.log"

# Email Configuration (Optional)
# EMAIL_FROM="noreply@yourapp.com"
EMAIL_FROM_NAME="JP Auth API"

# Crypto Configuration (Required for encryption/decryption)
CRYPTO_SECRET="your-64-character-hex-string-for-encryption-key-here-32bytes"
CRYPTO_IV="your-32-character-hex-string-16bytes"